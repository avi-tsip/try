const today = new Date();
const unixTime = today.getMilliseconds();

const person = {
    age: 20
};


class Color {

}

const red = new Color();
red.    //no attributes to color and that is why we get the error